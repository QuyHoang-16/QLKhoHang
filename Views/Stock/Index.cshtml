@using QuanLyKho.Models.Invent
@inject QuanLyKho.Data.ApplicationDbContext Context
@inject QuanLyKho.Services.INetcoreService Svc
@{
    ViewData["Title"] = "Stock Index";
    Layout = "_AdminlteNetcore";
    
    List<VMStock> stocks = new List<VMStock>();
    stocks = Svc.GetStockPerWarehouse();
   
}



<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">

    <!-- Main content -->
    <section class="content">

        <div class="row">
            <div class="col-lg-12">
                <div class="box box-primary">
                    <div class="box-header with-border">
                        <h3 class="box-title"><i class="fa fa-cube text-primary"></i> <span>Tồn kho theo kho (Realtime)</span></h3>
                        <div class="box-tools pull-right">
                            <div class="has-feedback">
                                <input type="text" id="mainSearch" class="form-control input-sm" placeholder="Tìm kiếm...">
                                <span class="glyphicon glyphicon-search form-control-feedback"></span>
                            </div>
                        </div>
                        <!-- /.box-tools -->
                    </div>

                    <div class="box-body no-padding">
                        <div class="mailbox-controls">
                            <div class="text-center">
                                <button type="button" id="btn-export-excel" class="btn btn-success btn-sm">
                                    <i class="fa fa-file-excel-o"></i> Xuất Excel
                                </button>
                                <button type="button" id="btn-export-pdf" class="btn btn-danger btn-sm">
                                    <i class="fa fa-file-pdf-o"></i> Xuất PDF
                                </button>
                                <button type="button" id="btn-export-csv" class="btn btn-primary btn-sm">
                                    <i class="fa fa-file-text-o"></i> Xuất CSV
                                </button>
                            </div>
                        </div>

                        <div class="table-responsive mailbox-messages">
                            <table class="table table-hover table-striped" style="width:100%">
                                <thead>
                                    <tr>
                                        <th><i class="fa fa-tags"></i> Sản phẩm</th>
                                        <th><i class="fa fa-cubes"></i> Kho</th>
                                        <th><i class="fa fa-truck"></i> SL Nhập</th>
                                        <th><i class="fa fa-plane"></i> SL Xuất</th>
                                        <th><i class="fa fa-download"></i> SL Chuyển vào</th>
                                        <th><i class="fa fa-upload"></i> SL Chuyển ra</th>
                                        <th class="text-aqua"><i class="fa fa-check-circle"></i> SL Tồn</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in stocks)
                                    {
                                        <tr>
                                            <td>@item.Product</td>
                                            <td>@item.Warehouse</td>
                                            <td>@item.QtyReceiving</td>
                                            <td>@item.QtyShipment</td>
                                            <td>@item.QtyTransferIn</td>
                                            <td>@item.QtyTransferOut</td>
                                            <td>@item.QtyOnhand</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>

        </div>

    </section>
    <!-- /.content -->
</div>
<!-- /.content-wrapper -->
@section Styles{

}


@section Scripts{
    <!-- ChartJS -->
    <script src="~/AdminLTE/bower_components/chart.js/Chart.js"></script>
    <script>
        $(document).ready(function () {
            var mainTable;

            mainTable = $('.table').DataTable({
                "lengthChange": false,
                "info": false,
                "searching": true,
                "dom": 'lrtip',
                "scrollX": false,
                "buttons": [
                                { 
                                    extend: 'csv',
                                    filename: 'stock-csv'
                                },
                                { 
                                    extend: 'pdf',      
                                    filename: 'stock-pdf'
                                },
                                { 
                                    extend: 'excel',
                                    filename: 'stock-excel'
                                }
                        ]
            });

            $('#mainSearch').keyup(function () {
                mainTable.search($(this).val()).draw();
            });


            $("#btn-export-csv").on("click", function () {
                mainTable.button('.buttons-csv').trigger();
            })

            $("#btn-export-excel").on("click", function () {
                mainTable.button('.buttons-excel').trigger();
            })

            $("#btn-export-pdf").on("click", function () {
                mainTable.button('.buttons-pdf').trigger();
            })



        });




    </script>
}


